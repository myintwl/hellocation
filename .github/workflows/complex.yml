name: complex job 

on: push

jobs:
 ubuntu:
   runs-on: ubuntu-latest
   steps:
   - run: date
 windows:
   runs-on: windows-latest
   steps:
   - run: date
 macos:
   runs-on: mac-latest
   steps:
   - run: date
 depends:
   needs: [ubuntu, windows, macos]
   runs-on: ubuntu-latest
   steps:
   - run: date